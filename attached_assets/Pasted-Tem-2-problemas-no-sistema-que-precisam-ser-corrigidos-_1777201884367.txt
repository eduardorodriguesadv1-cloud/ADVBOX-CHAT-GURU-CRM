Tem 2 problemas no sistema que precisam ser corrigidos:

PROBLEMA 1: Todos os leads aparecem como campanha "Indefinida"
- A função identifyCampaign() não está classificando os leads corretamente
- Leads existentes nunca foram processados retroativamente

AÇÕES:
1. Revisar a função identifyCampaign() em todos os matches:
   - "laudo do SUS aqui em PE" → LAUDO_SUS_PE
   - "auxílio doença" / "auxilio doença" / "auxílio-doença" / "auxilio-doenca" → AUX_DOENCA
   - "tenho interesse e queria mais informações" → LAUDO_SUS_GERAL
   - "auxílio-acidente" / "auxilio-acidente" / "auxílio acidente" / "auxilio acidente" → AUX_ACIDENTE
   - "passei pela perícia" / "passei pela pericia" / "negaram meu beneficio" / "negaram meu benefício" → PERICIA_NEGADA
   
2. Tornar o matching CASE-INSENSITIVE e mais flexível (usar normalize pra remover acentos antes de comparar)

3. Criar script de migração que:
   - Pega TODOS os leads com campaign = null OU campaign = "INDEFINIDA"
   - Pega a primeira mensagem (firstMessage) de cada um
   - Aplica identifyCampaign() retroativamente
   - Atualiza o campo campaign no banco

4. Rodar esse script de migração agora pra reclassificar os leads existentes

PROBLEMA 2: Todos os leads aparecem atribuídos ao "Thiago Tavares"
- O round-robin entre Thiago e Tammyres não está funcionando
- Suspeito que esteja usando o campo antigo `assigned_agent` (texto) em vez do novo `agent_id` (FK)

AÇÕES:
1. Verificar a lógica de distribuição automática quando lead chega via webhook
2. Implementar round-robin REAL alternando entre atendentes ATIVOS do team "COMERCIAL_TRAFEGO":
   - Manter contador `leads_received` em cada agente
   - Atribuir novo lead pro agente com MENOR contador
   - Em caso de empate, usar ordem de criação
3. Criar script de migração que:
   - Pega leads atuais que vieram pelo número Comercial (91850647)
   - Redistribui em round-robin entre Thiago e Tammyres
   - Atualiza o agentId
4. No frontend, exibir o nome do agente puxando da relação com a tabela `agents` (via agentId), não do campo legado `assigned_agent`

Por favor, executa as duas correções e roda os scripts de migração. Me avise quando terminar pra eu testar.