Quero expandir a integração com Meta Marketing API pra fazer DIAGNÓSTICO AUTOMÁTICO de toda a conta. Já tenho campanhas, adsets, ads e métricas. Agora quero:

NOVAS TABELAS:

1. custom_audiences (públicos customizados)
- id (serial PK)
- meta_audience_id (text, unique)
- name (text)
- type (text) — "CUSTOM" / "LOOKALIKE" / "WEBSITE" / "CUSTOMER_LIST" / "ENGAGEMENT" / "VIDEO" / "OFFLINE"
- subtype (text) — "ENGAGED_WITH_INSTAGRAM" etc
- approximate_count (integer) — tamanho estimado
- status (text) — "ready" / "in_progress" / "expired"
- description (text) — regras do público
- rules (jsonb) — regras completas
- created_time (timestamp)
- last_used_at (timestamp) — quando foi usado em adset pela última vez
- created_at, updated_at

2. audience_usage (relação adset → audience)
- adset_id (FK)
- audience_id (FK pra custom_audiences)
- type (text) — "INCLUDED" / "EXCLUDED" / "LOOKALIKE_BASE"

EXPANSÃO DA API META:

No serviço meta-ads.ts, adicionar:
- fetchCustomAudiences() — busca públicos customizados (/customaudiences)
- fetchAudienceDetails(audienceId) — detalhes completos
- syncAllAudiences() — sync de tudo

ALGORITMOS DE DIAGNÓSTICO (criar serviço analytics.ts):

1. detectDuplicateAudiences()
- Compara regras (rules JSON) entre públicos
- Marca os com regras idênticas como duplicatas
- Sugere qual deletar (manter o mais antigo OU o que tá em uso)

2. detectUnusedAudiences()
- Públicos NUNCA usados em adset
- OU não usados há 90+ dias

3. detectAudienceOverlap()
- Adsets que têm públicos sobrepostos
- Sinaliza canibalização

4. detectStaleAudiences()
- Em status "in_progress" há +30 dias

5. detectLowPerformanceAds()
- CTR < 0.5% há 14+ dias
- Frequency > 4 (fadiga)
- Spend > 50 sem conversão

6. detectDuplicateAds()
- Mesma imagem/vídeo
- Mesmo headline
- Marca pra consolidar

NOVA PÁGINA NO FRONTEND: /audit

Página "Auditoria Meta" com 4 abas:

ABA 1 — VISÃO GERAL
Card resumo com totais:
- X públicos (Y duplicados, Z não usados)
- X adsets (Y com sobreposição, Z com targeting amplo)
- X anúncios (Y baixo CTR, Z duplicados)
- Recomendações totais geradas

ABA 2 — PÚBLICOS
Tabela de públicos com colunas:
- Nome, Tipo, Tamanho, Status, Última utilização, Tags de problema (Duplicado/Não usado/Em preenchimento)
- Filtros pra cada tipo de problema
- Botão "Ver detalhes" mostra adsets que usam

ABA 3 — DIAGNÓSTICO DETALHADO  
Lista de problemas detectados em ordem de severidade:
- 🔴 Crítico: canibalização de público entre adsets
- 🟠 Alto: criativos com CTR < 0.5% há 14d
- 🟡 Médio: duplicatas de público
- 🔵 Baixo: públicos não usados há 90d

Cada item com:
- Descrição do problema
- Impacto estimado
- Sugestão de ação
- Botão "Ver no Meta Ads" (link direto)

ABA 4 — RECOMENDAÇÕES
Lista priorizada de "ações sugeridas":
1. "Deletar 5 públicos duplicados" → mostra quais
2. "Pausar 3 anúncios com baixo CTR" → mostra quais
3. "Reativar [MONOCULAR] STF BPC - era top performer (CPL R$1,15)"
4. etc

Cada uma com:
- Botão "Marcar como feito" (toggle de checked)
- Estimativa de impacto (R$ economia/aumento de leads)

PERMISSÕES NECESSÁRIAS:
A API de Custom Audiences precisa de "ads_management" + "business_management" (já tem).

IMPLEMENTAÇÃO:
1. drizzle push pras novas tabelas
2. Sync inicial de públicos
3. Roda algoritmos de diagnóstico
4. Frontend
5. Cron diário às 6h pra atualizar tudo automaticamente

Quando terminar, me avisa quantos problemas detectou.