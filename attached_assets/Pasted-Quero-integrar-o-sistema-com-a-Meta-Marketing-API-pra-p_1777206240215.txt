Quero integrar o sistema com a Meta Marketing API pra puxar dados das campanhas de tráfego do escritório automaticamente.

CONTEXTO:
- Já temos CRM rodando com leads do ChatGuru
- Agora quero cruzar os leads com campanhas do Meta Ads
- Pra ver CPL real e ROI por campanha

CREDENCIAIS (vou colocar como variáveis de ambiente no Replit):
- META_ACCESS_TOKEN (vou cadastrar nas Secrets)
- META_AD_ACCOUNT_ID = act_6541320839657528

TAREFAS:

1. CRIAR NOVAS TABELAS NO BANCO:

a) campaigns
- id (serial PK)
- meta_campaign_id (text, unique) — ID da campanha no Meta
- name (text)
- objective (text) — LEADS, ENGAGEMENT, etc.
- status (text) — ACTIVE, PAUSED, etc.
- daily_budget (decimal)
- created_at, updated_at

b) campaign_metrics_daily
- id (serial PK)
- campaign_id (FK pra campaigns)
- date (date)
- spend (decimal)
- impressions (integer)
- reach (integer)
- conversations_started (integer) — mensagens iniciadas
- cost_per_conversation (decimal)
- created_at

2. CRIAR INTEGRAÇÃO COM META MARKETING API:

Criar arquivo lib/db/src/services/meta-ads.ts com funções:
- fetchCampaigns() — busca todas as campanhas da conta
- fetchCampaignMetrics(campaignId, dateFrom, dateTo) — busca métricas
- syncAllCampaigns() — sincroniza tudo

Endpoint base da Meta:
https://graph.facebook.com/v25.0/{ad_account_id}

Usar fetch nativo. Autenticação via query param ?access_token={META_ACCESS_TOKEN}.

3. CRIAR CRON JOB:

Roda toda manhã às 7h (horário Brasília):
- Busca todas as campanhas ativas
- Pra cada uma, busca métricas dos últimos 30 dias
- Atualiza tabelas campaigns e campaign_metrics_daily
- Faz upsert (não duplica se já tiver dados daquele dia)

4. CRIAR ENDPOINTS:

- GET /api/campaigns — lista todas as campanhas com métricas agregadas
- GET /api/campaigns/:id — detalhes de uma campanha
- GET /api/campaigns/sync — força sync manual
- GET /api/dashboard/campaign-performance — performance combinada (Meta + leads do CRM)

5. NOVA PÁGINA NO FRONTEND: "Performance de Tráfego"

Adicionar no menu lateral, em CONFIGURAÇÃO. Mostra:

- Cards no topo: Total investido nos últimos 30 dias / Total de leads / CPL médio / ROI estimado
- Tabela de campanhas com colunas:
  - Nome da campanha
  - Status (Ativa/Pausada)
  - Investido (R$)
  - Impressões
  - Conversas iniciadas (do Meta)
  - CPL Meta (custo / conversas)
  - Leads no CRM (cr